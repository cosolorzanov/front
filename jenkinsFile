pipeline {
    agent {label 'app'}
    stages {
        stage('build') {
            steps {
                sh 'docker build -t front .'
            }
        }
        stage('run') {
            steps {
                sh 'docker run --name front --rm -p 80:4200 front'
            }
        }
    }
}
