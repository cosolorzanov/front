pipeline {
    agent {label 'app'}
    stages {
        stage('build') {
            steps {
                sh 'docker build -t front .'
            }
        }
        stage('run') {
            steps {
                sh 'docker --name front --rm run -p 80:4200 front'
            }
        }
    }
}
